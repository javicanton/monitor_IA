server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  # Healthcheck
  location = /health { return 200 'ok'; add_header Content-Type text/plain; }

  # Evitar cache del HTML para no servir builds antiguos
  location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    add_header Pragma "no-cache";
    add_header Expires "0";
    try_files $uri =404;
  }

  # Static/SPA
  location / {
    try_files $uri /index.html;
  }

  # (Opcional) Proxy al backend si usas /api
  location /api/ {
    proxy_pass http://backend:5001/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_connect_timeout 30s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    send_timeout 300s;
  }

  # Proxy para recursos que el frontend consume fuera de /api
  # (p. ej. /topics se sirve desde el backend Flask)
  location = /topics {
    proxy_pass http://backend:5001/topics;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 30s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    send_timeout 300s;
  }
}
